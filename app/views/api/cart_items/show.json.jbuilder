json.extract! @cart_item, :id, :shopper_id, :product_id, :product_quantity, :product
# @cart_item.product do
#                 json.id @cart_item.product.id
#                 json.title @cart_item.product.title
#                 json.price @cart_item.product.price
#                 json.inventory @cart_item.product.inventory
#                 json.rating @cart_item.product.rating
#                 json.photoUrl @cart_item.product.photoUrl
#             end
